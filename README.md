# English Quiz (Grade 4–12)

Minimal English quiz app for Chinese students using **Next.js (App Router)** + **Tailwind CSS**.

## Run locally

1) Install Node.js (LTS) and reopen terminal.
2) Install deps:

```bash
npm install
```

3) Start dev server:

```bash
npm run dev
```

Then open `http://localhost:3000`.

